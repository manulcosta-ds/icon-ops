{
  "name": "icon-guardian",
  "version": "1.0.0",
  "description": "Figma plugin for icon library management and auditing",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "verify": "node verify.js",
    "test": "node verify.js",
    "build:vite": "vite build",
    "watch:vite": "vite build --watch"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vite-plugin-singlefile": "^0.13.5"
  }
}
